subscription PubKeysSubscription {
	pubKeysSubscription {
		id
		key
		pubId
	}
}

subscription PubsSubscription {
	pubsSubscription {
		capacity
		id
		isActive
		occupancy
		pubId
		queueStatus
		themeId
	}
}

subscription ThemesSubscription {
	themesSubscription {
		color
		displayName
		id
		logo
		themeId
	}
}
